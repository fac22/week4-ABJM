const model = require('../database/model');
const template = require('../template');

function post(request, response) {
	//sid
	// getUser
	// getPost if correct user
	// deletePost
	// redirect where?
	// error message
}

module.exports = { post };
